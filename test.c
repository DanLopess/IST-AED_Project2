void calculateLateStart(List x){
	link i,f;
	for(i = x->head; i != NULL; i=i->next){
		Key lowestValue;
		int firstLowestValueCalculated = 0;
		link tempNode;

		for(f = i->next; f!=NULL; f=f->next){
			tempNode = findNode(((Item)f->item)->dependencies), ((Item)i->item)->id, 1)
			if(tempNode != NULL){
				if(!firstLowestValueCalculated)
					lowestValue = lateStart - duration
				else
			}
		}
	}
}
